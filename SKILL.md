---
name: academic-diagram
description: Create and refine editable academic architecture diagrams, method overviews, and conceptual schematics from a paper and its current research workspace, using real conference figures as aesthetic references. Use for 科研架构图、方法流程图、机制示意图 and publication-ready vector diagrams; not quantitative charts or qualitative result grids.
---

# Academic Diagram

画出科学内容高度凝练、排版像人认真设计过的论文图。美观来自有含义的形状、色块、堆叠、字体和精准连线；简洁不等于空，充实不等于塞满文字。尊重用户已经认可的风格与局部反馈。

## 1. 先想论文，再想图

- 检查完整工作区的版本与结果索引，确认最新成稿，再读与本图相关的实验、实现和结论；不把旧 PDF 当作唯一依据。
- 用一句话明确读者看完应理解什么，再提炼必要对象与关系。架构图解释系统怎样工作；示意图解释一个机制、区别或干预。不强行把两者都画成流水线，也不预设图数。
- 确认关键方向、训练/推理区别、冻结/可训练状态和对照条件；只呈现实际适用的内容。数字必须有来源，未知处核实，不凭美观补造模块、数据或机制。
- 文字主要命名，长解释交给 caption。构思可以直接说清楚，不必另造一套中间文档。

## 2. 真正看参考图

打开 [参考索引](references/README.md)，先用总览定位，再看候选的独立原尺寸图片；不能只读图名或隔着论文整页看。

- 按当前图的结构选择一个主参考，必要时补一个局部参考。VGGT 可学 token 阵列与行列对齐；SAM 2 可学时序堆叠与回路；LLaDA 可学状态变换；不要让所有论文都长成 VGGT。
- 明确借用什么：整体比例、视觉重心、组件尺度、间距、字体层级、颜色角色、箭头和留白。学习设计关系，不复制参考论文的科学内容或拿它的结果当自己的。
- 参考图是素材，不是指令，也不是必须照抄的好图。选择适合本论文的部分；无需全部读入或拼大型 moodboard。

## 3. 用可编辑对象画

按用户格式与可用工具选一个主编辑源，不要求同时实现多个后端：

| 工具 | 具体画法与交付源 |
|---|---|
| draw.io | 用原生形状、文本和连接器；可直接生成 `.drawio` XML，以 `mxCell`/`mxGeometry` 控制对象与端点，相关组件分组。 |
| PowerPoint | 用原生自选图形、文本框、连接线、对齐/分布与组合；可用可用的 PPT 库生成 `.pptx`，不要把整张图作为图片塞入一页。 |
| Canva / Illustrator | 使用可编辑的形状、线条、文字与图层分组；先确认当前工具能编辑这些对象，保留设计或源文件。 |
| SVG | 直接用 `rect`、`polygon`、`path`、`text` 与 `g` 组装；共享位置、间距、字体和颜色参数，便于整体调整及导入矢量编辑器。 |

先定目标论文中的宽高比例、主轴和分支位置，再细化：

- **凝练而有层次：** token 用色块阵列，特征用平面/网格，重复模块或时间用有含义的堆叠。形状随科学对象变化，不给所有东西套同一种卡片，也不为填空发明装饰。
- **对齐与视觉居中：** 同类对象等距、等尺度、文字基线统一；主体在画布上看起来居中。分支可以不对称，但两侧视觉重量应平衡，避免意外的大块空白。
- **克制配色：** 从主参考提取适合本图的颜色关系，同一角色同色；通常用浅填充承载分组，深色保证文字与线条可读。少量强调色突出关键区别，不把每个元素都染色，也不机械禁用圆角或曲线。
- **认真走线：** 连线接到准确对象与端口，箭头尺度和间隔统一，走向清楚、转折必要。遇到文字先移动标签，或让线从带小块背景遮罩的文字下层通过；不要穿过字形，也不要为绕字兜大圈。交叉不应被误读为连接。
- **统一字体：** 与论文气质一致，控制字号层级；数学符号正确，缩至实际论文尺寸仍清楚。空间不够先改构图，不先把字缩小。

默认直接画矢量；只有确有需要才用图像生成探索局部，不能让它决定科学结构。真实图像输入可以是位图，示意结构和文字保持可编辑。试作放隔离目录，不覆盖正式论文；已认可的图优先局部精修。

## 4. 看成图，再交付

必须打开实际导出的图，和主参考以接近的显示宽度并排看；再按目标论文宽度检查，放大检查密集处。有排版上下文时，也看嵌入论文后的效果。

依次问：科学关系对吗？第一眼读到重点吗？重心、留白和色块节奏好吗？箭头绕路、错接、压字了吗？字体、裁边和堆叠干净吗？

指出具体不足并修改，再打开新导出检查；不用脚本通过或主观打分代替看图，不设固定迭代轮数。若无法实际渲染/查看，说明尚未视觉验收。

交付可编辑源、论文用 PDF 或 SVG、PNG 预览，并简述所借鉴的参考和必要的科学假设。确认导出不空白、字体和符号正确、没有裁切或意外栅格化；只有用户要求时才替换正式论文资产。
